'use client';\n\nimport { useState } from 'react';\nimport { Pessoa, CreatePessoaDTO, UpdatePessoaDTO } from '@/lib/types/pessoa';\nimport { formatDateBR, parseDateBR } from '@/lib/utils/date';\n\ninterface PessoaFormProps {\n  pessoa?: Pessoa;\n  onSubmit: (data: CreatePessoaDTO | UpdatePessoaDTO) => Promise<void>;\n  isLoading?: boolean;\n  error?: string;\n}\n\nexport default function PessoaForm({ pessoa, onSubmit, isLoading = false, error }: PessoaFormProps) {\n  const [formData, setFormData] = useState({\n    nome: pessoa?.nome || '',\n    email: pessoa?.email || '',\n    telefone: pessoa?.telefone || '',\n    dataNascimento: pessoa?.dataNascimento || '',\n  });\n\n  const [validationErrors, setValidationErrors] = useState<Record<string, string>>({});\n\n  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {\n    const { name, value } = e.target;\n    setFormData((prev) => ({\n      ...prev,\n      [name]: value,\n    }));\n    // Clear validation error for this field\n    if (validationErrors[name]) {\n      setValidationErrors((prev) => {\n        const newErrors = { ...prev };\n        delete newErrors[name];\n        return newErrors;\n      });\n    }\n  };\n\n  const handleSubmit = async (e: React.FormEvent) => {\n    e.preventDefault();\n    setValidationErrors({});\n\n    try {\n      const submitData = {\n        nome: formData.nome.trim(),\n        email: formData.email.trim(),\n        ...(formData.telefone && { telefone: formData.telefone.trim() }),\n        ...(formData.dataNascimento && { dataNascimento: formData.dataNascimento.trim() }),\n      };\n\n      await onSubmit(submitData);\n    } catch (err: any) {\n      if (err.details) {\n        setValidationErrors(err.details);\n      }\n    }\n  };\n\n  return (\n    <form onSubmit={handleSubmit} className=\"space-y-4\">\n      {error && (\n        <div className=\"p-4 bg-red-100 border border-red-400 text-red-700 rounded\">\n          {error}\n        </div>\n      )}\n\n      <div>\n        <label htmlFor=\"nome\" className=\"block text-sm font-medium text-gray-700\">\n          Nome *\n        </label>\n        <input\n          type=\"text\"\n          id=\"nome\"\n          name=\"nome\"\n          value={formData.nome}\n          onChange={handleChange}\n          disabled={isLoading}\n          className={`mt-1 block w-full px-3 py-2 border rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 ${\n            validationErrors.nome ? 'border-red-500' : 'border-gray-300'\n          }`}\n          required\n        />\n        {validationErrors.nome && (\n          <p className=\"mt-1 text-sm text-red-600\">{validationErrors.nome}</p>\n        )}\n      </div>\n\n      <div>\n        <label htmlFor=\"email\" className=\"block text-sm font-medium text-gray-700\">\n          Email *\n        </label>\n        <input\n          type=\"email\"\n          id=\"email\"\n          name=\"email\"\n          value={formData.email}\n          onChange={handleChange}\n          disabled={isLoading}\n          className={`mt-1 block w-full px-3 py-2 border rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 ${\n            validationErrors.email ? 'border-red-500' : 'border-gray-300'\n          }`}\n          required\n        />\n        {validationErrors.email && (\n          <p className=\"mt-1 text-sm text-red-600\">{validationErrors.email}</p>\n        )}\n      </div>\n\n      <div>\n        <label htmlFor=\"telefone\" className=\"block text-sm font-medium text-gray-700\">\n          Telefone\n        </label>\n        <input\n          type=\"tel\"\n          id=\"telefone\"\n          name=\"telefone\"\n          value={formData.telefone}\n          onChange={handleChange}\n          disabled={isLoading}\n          placeholder=\"(XX) XXXXX-XXXX\"\n          className={`mt-1 block w-full px-3 py-2 border rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 ${\n            validationErrors.telefone ? 'border-red-500' : 'border-gray-300'\n          }`}\n        />\n        {validationErrors.telefone && (\n          <p className=\"mt-1 text-sm text-red-600\">{validationErrors.telefone}</p>\n        )}\n      </div>\n\n      <div>\n        <label htmlFor=\"dataNascimento\" className=\"block text-sm font-medium text-gray-700\">\n          Data de Nascimento\n        </label>\n        <input\n          type=\"date\"\n          id=\"dataNascimento\"\n          name=\"dataNascimento\"\n          value={formData.dataNascimento}\n          onChange={handleChange}\n          disabled={isLoading}\n          className={`mt-1 block w-full px-3 py-2 border rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 ${\n            validationErrors.dataNascimento ? 'border-red-500' : 'border-gray-300'\n          }`}\n        />\n        {validationErrors.dataNascimento && (\n          <p className=\"mt-1 text-sm text-red-600\">{validationErrors.dataNascimento}</p>\n        )}\n      </div>\n\n      <div className=\"flex gap-2\">\n        <button\n          type=\"submit\"\n          disabled={isLoading}\n          className=\"px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 disabled:bg-gray-400 disabled:cursor-not-allowed\"\n        >\n          {isLoading ? 'Salvando...' : pessoa ? 'Atualizar' : 'Criar'}\n        </button>\n        <a\n          href=\"/pessoas\"\n          className=\"px-4 py-2 bg-gray-300 text-gray-800 rounded-md hover:bg-gray-400\"\n        >\n          Cancelar\n        </a>\n      </div>\n    </form>\n  );\n}\n"